program Program
{
	int sum(int a[], int n)
	int s, i;
	{
		s = 5;
		// for(i = 0; i<n;i++)
		// {
		// 	s += a[i];
		// }
		return s + n;
	}
	void main() 
	int t, n, a; 
	bool b;
	int x[]; 
	{
		n = 10;
		b = true;
		a = 3;
		t = 2;
		//read(n);
		if( t == 2 || n < 10 && n >= 5 && a < 2 || b == true) {
			x = new int[n];
			print('t', 3);
		}
		// a = new int[5];
		// for(i = 0; i < n; i++) {
		// 	x[i] = i;
		// }
		sum(x, n);
		print('n', 2);
		print(n);
	}
}
